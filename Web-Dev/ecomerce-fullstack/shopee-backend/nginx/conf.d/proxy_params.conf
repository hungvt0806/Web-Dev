# ============================================================
#  Shared proxy parameters — included in every proxy_pass block
# ============================================================

# Pass real client IP to Spring Boot
proxy_set_header X-Real-IP         $remote_addr;
proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
proxy_set_header X-Forwarded-Host  $host;
proxy_set_header Host              $host;

# Disable proxy buffering for streaming responses
proxy_buffering       off;
proxy_request_buffering off;

# Timeouts
proxy_connect_timeout  10s;
proxy_send_timeout     60s;
proxy_read_timeout     60s;

# HTTP 1.1 + keepalive to upstream
proxy_http_version 1.1;
proxy_set_header Connection "";

# Pass Spring trace ID back to client
add_header X-Trace-Id $upstream_http_x_trace_id always;
